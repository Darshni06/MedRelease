# MedRelease frontend

React + Vite + Tailwind CSS. See the root `README.md` for full setup and
deployment instructions. Quick reference:

```bash
npm install
cp .env.example .env
npm run dev
```

Set `VITE_API_URL` in `.env` to point at your backend (defaults to
`http://localhost:8000`).
